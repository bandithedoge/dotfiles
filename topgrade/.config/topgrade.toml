# send desktop notifications for every step
notify_each_step = true

# clean cache
cleanup = true

# custom commands
[commands]
"Orphan Cleanup" = "pacman -Rs $(pacman -Qtdq)" # remove orphaned packages
